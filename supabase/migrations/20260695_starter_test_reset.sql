-- ============================================================
-- 내 포켓몬 테스트 데이터 초기화 — 모든 user_starter row 삭제.
--
-- 의도: 기획 검토용 테스트 등록 데이터를 비워서, 모든 유저가 다시
-- /my-pokemon 진입 시 뽑기 단계부터 시작하도록.
--
-- 멱등: user_starter 가 비어 있으면 영향 없음 (TRUNCATE 또는 DELETE
-- 둘 다 안전). FK on delete cascade 가 별도로 user_starter 를 참조하지
-- 않으므로 다른 데이터 영향 없음.
-- ============================================================

delete from user_starter;
